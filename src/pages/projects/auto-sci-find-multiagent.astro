---
import { ArrowLeft } from "lucide-astro";
import SiteLayout from "../../layouts/SiteLayout.astro";

const versions = [
  {
    name: "参赛阶段的科研闭环",
    goal: "从研究问题推进到可验证的实验报告",
    agents: "ResearchAgent / SurveyAgent / ReflectionAgent / CoderAgent"
  },
  {
    name: "赛后深化的调研系统",
    goal: "从研究方向或种子论文构建领域谱系地图",
    agents: "SurveyAgent / LineageAgent / PaperReaderAgent / SurveyCriticAgent"
  }
];

const agents = [
  ["SurveyAgent", "检索、筛选、解析和精读论文，提炼谱系、baseline、数据集、指标与 gap", "survey report"],
  ["ReflectionAgent", "检查调研覆盖、实验充分性与创新性，决定通过或打回", "quality state"],
  ["CoderAgent", "搭建环境、复现 baseline、执行优化与对照实验并记录结果", "experiment report"],
  ["ResearchAgent", "维护全局任务状态，完成跨 Agent 调度和最终汇总", "state / handoff"]
];

const milestones = [
  ["2026.06.06", "完成基础 LLM tool-use loop、工具抽象和 Skill 加载机制"],
  ["2026.06.07", "打通检索、筛选、下载、解析、精读和综合流程，并加入 CoderAgent"],
  ["2026.06.08", "将专职 Agent 暴露成主控工具，加入 ReflectionAgent 和后台任务"],
  ["2026.06.09–10", "增加持久任务图、handoff、上下文压缩、大结果落盘和分页读文件"],
  ["2026.06.11", "修复后台任务提前结束与空返回，加入引用扩展和二次筛选"],
  ["2026.06.30+", "收敛为文献调研系统，加入谱系、精读、文献矩阵和证据闸门"]
];
---

<SiteLayout
  title="Auto-Sci-Find Multi-Agent"
  description="从 Research Seed 到实验报告：AI4S 竞赛中的自动化科研 Multi-Agent 框架，以及赛后深化的 SurveyAgent 文献谱系系统。"
  image="/images/project-research.webp"
>
  <header class="project-detail-hero shell reveal">
    <a class="project-back-link" href="/projects/">
      <ArrowLeft size={15} aria-hidden="true" /> 返回项目
    </a>
    <p class="eyebrow">AI4S / Research automation</p>
    <h1>Auto-Sci-Find<br />Multi-Agent</h1>

    <div class="project-detail-summary">
      <p class="project-detail-lead">
        从 Research Seed 出发，把文献调研、代码实验、质量检查和反思迭代连接成可执行、可追踪、可回退的自动化科研闭环；赛后继续把我负责的调研部分深化为面向领域谱系地图的 SurveyAgent 系统。
      </p>
      <dl class="project-facts">
        <div><dt>Period</dt><dd>2026.04–06</dd></div>
        <div><dt>Context</dt><dd>AI4S Challenge</dd></div>
        <div><dt>Role</dt><dd>Survey Pipeline</dd></div>
        <div><dt>Result</dt><dd>Preliminary 1st</dd></div>
      </dl>
    </div>

  </header>

  <article class="prose project-article">
    <p class="project-article-deck">
      这是一套持续演化的系统：参赛阶段覆盖文献调研、代码实验和反思迭代；赛后则把文献调研部分单独做深，目标从“搜到相关论文”推进到“建立可复用、可检查的领域谱系地图”。
    </p>

    <h2>先说明两个版本</h2>
    <div class="article-table-wrap">
      <table>
        <thead><tr><th>阶段</th><th>目标</th><th>主要 Agent</th></tr></thead>
        <tbody>
          {versions.map((version) => (
            <tr>
              <td>{version.name}</td>
              <td>{version.goal}</td>
              <td>{version.agents}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>

    <p>两版共用 BaseAgent、工具注册、Skill 加载、上下文管理和文件化产物等基础设施。参赛阶段负责完整科研闭环，赛后阶段则将范围收窄为可靠的文献调研系统。</p>

    <blockquote>贡献边界：比赛名次和完整 Multi-Agent 系统是团队成果；我主要负责文献调研链路及其后续演化，包括查询规划、检索筛选、引用扩展、论文解析与分析、文献矩阵和调研质量闭环。</blockquote>

    <h2>为什么要做这套系统</h2>
    <p>真正的科研任务很少能在一个 prompt 里结束。研究问题需要经历问题拆解、文献调研、baseline 选择、环境搭建、实验执行和结果反思；任何一环的信息不完整，后面的工作都可能建立在错误假设上。</p>
    <p>我们探索的核心问题不是怎样让单个 Agent 更聪明，而是能否把任务拆解、策略规划、调研分析、代码实验、结果校验和反思迭代串成一条可执行、可追踪、可回退的自动化科研闭环。</p>
    <p>这套框架后来用于 AI4S 智能体 CNS 挑战赛中的蛋白质构象系综生成任务。据团队参赛记录，我们以初赛第一名进入复赛。</p>

    <h2>科研闭环：交接比角色数量重要</h2>
    <figure class="article-figure">
      <img src="/images/yxy-agent-blog-assets/research-agent-workflow.png" alt="从 Research Seed 到实验报告的自动化科研 Multi-Agent 工作流" width="447" height="252" loading="lazy" />
      <figcaption>参赛阶段的自动化科研 Multi-Agent 工作流</figcaption>
    </figure>
    <p>最上层的 ResearchAgent 是调度者。它负责拆解任务、选择策略、交接上游产物，并根据质量检查决定继续、回退或结束。子 Agent 通过 Markdown、JSON、CSV、实验日志和模型输出交接证据，而不是只返回一段自然语言摘要。</p>

    <div class="article-table-wrap">
      <table>
        <thead><tr><th>Agent</th><th>主要职责</th><th>关键产物</th></tr></thead>
        <tbody>
          {agents.map((agent) => <tr>{agent.map((cell) => <td>{cell}</td>)}</tr>)}
        </tbody>
      </table>
    </div>

    <h2>一次任务怎样从头跑到尾</h2>
    <h3>定义 Research Seed</h3>
    <p>系统先把用户想法整理成准备验证的研究问题、使用场景、约束和成功标准。输入仍然模糊时，ResearchAgent 只提出必要的澄清问题，而不是立刻开始大规模搜索或训练。</p>

    <h3>SurveyAgent 建立证据底座</h3>
    <p>SurveyAgent 完成检索规划、论文搜索、筛选、PDF 下载与解析、关键论文精读和综合报告。报告必须给出可复现的代码与 baseline、数据集和指标、方法差异、失败场景，以及证据与待验证推断的边界。</p>

    <h3>Reflection 检查调研是否足够</h3>
    <p>ReflectionAgent 检查关键词覆盖、benchmark、可复现资源、指标和 baseline 是否明确。如果状态是 <code>needs_survey</code>，具体缺口会被交回 SurveyAgent，直到调研能够形成清晰的实验交接。</p>

    <h3>收敛 Hypothesis 与最小实验协议</h3>
    <p>ResearchAgent 基于调研材料明确当前 hypothesis、baseline、评价指标和最低验证成本，防止 CoderAgent 直接开始凭感觉优化。</p>

    <h3>CoderAgent 复现和优化</h3>
    <p>CoderAgent 读取 survey report 与 handoff，完成环境搭建、baseline 复现、profiling、单变量优化和 A/B 对照。依赖版本、随机种子、日志、结果与失败原因都会写入实验目录；长时间训练使用后台任务执行。</p>

    <h3>第二次 Reflection 校验结果</h3>
    <p>实验后的 ReflectionAgent 检查复现、公平对照、数据量和创新性。需要补实验时返回 <code>needs_coder</code>；假设不成立时回到调研或问题定义，而不是只挑好看的结果。</p>

    <h3>形成可审计的实验报告</h3>
    <p>最终产物不是自动生成的论文，而是一份包含研究问题、上游证据、环境、实验设置、结果、失败案例、结论边界与下一步工作的实验报告。</p>

    <h2>我负责的文献调研链路</h2>
    <p>如果文献调研要真正驱动实验，就必须同时解决召回、筛选和可追溯性。我的工作重点是让系统知道应该搜什么、哪些论文值得精读、已有方法之间是什么关系，以及怎样把结论交给后续实验。</p>

    <h3>查询规划与多源搜索</h3>
    <p><code>plan_queries</code> 将主题拆成任务定义、方法名、数据集、benchmark、系统名、作者群、相邻术语和年份窗口。搜索层组合 arXiv、OpenAlex、Semantic Scholar 和 Tavily 等来源，统一元数据、去重，并保留来源 query 与 source status。</p>

    <h3>两阶段筛选</h3>
    <p>候选池先经过可解释的启发式粗筛，综合题名和摘要重合、年份、引用、venue、来源共识与 PDF 可用性；随后由 LLM 对小批论文做语义相关性判断，以平衡召回、成本和新旧论文偏差。</p>

    <h3>从引用扩展到谱系追踪</h3>
    <p>关键词检索只能找到“名字像”的论文，不一定能找到真正定义问题或改变路线的工作。参赛后期加入的 <code>ref-expand</code> 会沿参考文献回溯；赛后演化出的 LineageAgent 同时追踪前作和后续引用，并表达 <code>introduces / extends / critiques / benchmarks / applies</code> 等关系。</p>
    <p>系统会区分 origin、concept、survey、method、benchmark、application、recent variant、side branch 与 noise。目标不再是收集尽可能多的论文，而是解释领域从哪里来、关键分叉在哪里、近期发生了什么变化。</p>

    <h3>PDF 解析、精读与文献矩阵</h3>
    <p>PDF 优先通过 MinerU 转成 Markdown，但“已经解析”不等于“已经理解”。PaperReaderAgent 负责主干节点精读，提取谱系角色、相对前作的变化、数据集、指标、证据、局限、代码和新的补搜目标。</p>
    <p>中心数据结构 <code>literature_matrix.csv</code> 一行对应一篇论文，记录标识符、引用数、谱系角色、方法分支、贡献、delta、数据集、指标、代码、复现性、优先级和证据状态。它既是给人看的领域地图，也是后续 Agent 的结构化输入。</p>

    <h3>质量闸门改变控制流</h3>
    <p><code>evidence-verify</code> 检查文件、标识符、年份覆盖和精读产物；SurveyCriticAgent 检查主线、分支和 gap 是否有证据。状态可能是 <code>pass</code>、<code>needs_search</code>、<code>needs_reading</code>、<code>needs_metadata</code> 或 <code>preliminary</code>。</p>
    <blockquote>反思不是报告末尾的一段自我批评，而是会改变控制流的程序状态。</blockquote>

    <h2>从科研闭环到文献调研专精版</h2>
    <figure class="article-figure article-figure-wide">
      <img src="/images/yxy-agent-blog-assets/survey-agent-flow.png" alt="SurveyAgent 文献检索、谱系追踪、论文精读与质量检查工作流" width="1920" height="1080" loading="lazy" />
      <figcaption>赛后深化的 SurveyAgent 文献谱系工作流</figcaption>
    </figure>
    <p>参赛阶段强调 <code>Survey → Reflection → Coder → Reflection</code> 的完整实验闭环；赛后深化阶段则移除代码实验环节，把范围收窄为从研究方向或种子论文构建可复用领域地图，并由 SurveyAgent 统一负责调度与最终汇总。</p>

    <div class="article-table-wrap">
      <table>
        <thead><tr><th>时间</th><th>主要变化</th></tr></thead>
        <tbody>{milestones.map((item) => <tr><td>{item[0]}</td><td>{item[1]}</td></tr>)}</tbody>
      </table>
    </div>

    <h2>支撑长任务的工程设计</h2>
    <h3>共享 BaseAgent</h3>
    <p>所有 Agent 复用同一套消息历史、LLM 调用、tool use、异常处理与上下文压缩循环。专职角色主要由 system prompt、可用 Skills 和产物约束定义，业务角色与运行时保持解耦。</p>

    <h3>Skill 按需加载</h3>
    <p>工具注册表只把 Skill 名称和简介放入 system prompt；Agent 真正需要时再读取完整说明，避免文献搜索、PDF 解析和实验评估的长说明常驻上下文。</p>

    <h3>持久状态和文件化记忆</h3>
    <p>短期计划由 Todo 管理，跨阶段任务写入 <code>research_state.json</code>；<code>handoff.md</code> 保存研究种子、hypothesis、baseline、最新反思和实验结果。即使任务中断或切换 Agent，也可以从文件恢复。</p>

    <h3>上下文压缩仍然保留审计记录</h3>
    <p>运行时会把完整 transcript 和超大工具输出落盘，只在上下文中保留路径、预览与连续性摘要。如果 API 报 prompt too long，系统会主动压缩并重试，而不是静默丢弃原始信息。</p>

    <h3>失败状态是一等公民</h3>
    <p>后台任务未结束时主循环不能提前完成；空返回会触发恢复提示；大结果自动落盘；Critic 的 blocking gap 会转成下一轮任务队列。这些规则没有增加新的智能角色，却决定了系统能否长时间运行。</p>

    <h2>怎样判断调研真的完成</h2>
    <p>生成 report 不等于任务完成。截至 2026 年 7 月 30 日的快照，DAG/区块链并发方向已有 58 行去重文献、20 份 PDF 和 6 份精读笔记，但状态仍是 <code>needs_reading</code>；大规模网格提取方向已有 40 行记录、12 份 PDF 和 15 份精读笔记，仍因元数据和高优先级覆盖不足而处于 <code>preliminary</code>。</p>
    <p>这反而说明质量门正在工作。可靠的 Agent 应该知道自己缺什么，并把“未完成”表达成具体、可执行的状态，而不是用流畅文字掩盖证据缺口。</p>

    <ul>
      <li>起源论文、关键概念、主要方法分支和近期代表作是否形成主线？</li>
      <li>新增关键词和引用是否仍会改变主线，还是只增加旁支？</li>
      <li>高优先级论文是否真正具备全文精读笔记？</li>
      <li>gap 和 idea 能否回指到论文、Web 或代码证据？</li>
      <li>对最新工作和 SOTA 的判断是否实际检索了当前年份？</li>
      <li>元数据、来源、复现资源和实验指标是否可追溯？</li>
    </ul>

    <h2>这次实践带给我的认识</h2>
    <h3>Multi-Agent 的价值来自约束</h3>
    <p>角色必须对应不同的工具权限、输入产物、输出格式和通过条件。如果多个 Agent 只是轮流输出文字，系统只会更慢、更贵。</p>

    <h3>科研自动化的核心是证据流</h3>
    <p>从论文到 hypothesis，从 hypothesis 到实验，从实验到结论，每一步都要保留来源。自然语言负责解释，JSON、CSV、日志和报告负责承载状态。</p>

    <h3>文献调研是持续的召回修复</h3>
    <p>精读可能暴露新的前作、数据集和作者群；Critic 发现断裂分支后又会生成新 query。搜索不是流水线最前面的一次性步骤，而是贯穿调研的反馈环。</p>

    <h3>Agent 必须能诚实地停在 preliminary</h3>
    <p>自动化系统最危险的不是报错，而是证据不足时仍生成完整、肯定的答案。明确的未完成状态通常比继续优化 prompt 更重要。</p>

    <h2>当前边界与下一步</h2>
    <p>文献召回仍受搜索 API 和元数据质量影响，PDF 解析可能丢失公式或表格，论文关系判断也必须由引用或正文证据约束。当前调研系统不再执行代码实验，完整科研闭环需要重新接入实验 Agent。</p>
    <ol>
      <li>建立标准化评测集，量化搜索召回、谱系完整性、精读准确性和 gap 可追溯性。</li>
      <li>加强论文实体消歧与引用图构建，减少预印本和正式版本造成的重复与断链。</li>
      <li>重新打通“文献证据 → 实验协议 → 结果 → 反向补搜”，同时保留证据闸门和 preliminary 机制。</li>
    </ol>

    <h2>写在最后</h2>
    <p>回看这个项目，我最看重的不是做了几个 Agent，而是逐渐把一个模糊的科研过程变成可执行的状态机：每个阶段有输入、有产物、有检查条件，也有失败后的回退路径。</p>
    <p>据团队参赛记录，这套框架在 AI4S 挑战赛中承担了问题拆解、策略选择、实验调度和反馈迭代，团队以初赛第一名进入复赛。对我而言，文献调研子系统的持续演化更让我确信：科研 Agent 真正需要的不是更会写答案，而是更会组织证据、承认缺口，并知道下一步该做什么。</p>
  </article>
</SiteLayout>
