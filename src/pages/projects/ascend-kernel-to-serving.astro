---
import { ArrowLeft } from "lucide-astro";
import SiteLayout from "../../layouts/SiteLayout.astro";

const pipeline = [
  {
    label: "Foundations",
    title: "MatMul / Softmax",
    copy: "验证 Triton-Ascend 的基础开发能力、并行划分、访存组织与数值稳定性。"
  },
  {
    label: "Attention",
    title: "Paged Attention",
    copy: "处理分页 KV Cache、GQA 头映射、变长序列、无效 token mask 与 Online Softmax。"
  },
  {
    label: "Long Context",
    title: "Quest Sparse",
    copy: "先筛选相关逻辑块，再执行精确注意力，降低长上下文下的 KV 读取和计算成本。"
  },
  {
    label: "System",
    title: "vLLM-Ascend",
    copy: "把算子接入真实推理链路，并以正确性、离线吞吐、Serving 和图模式逐层验证。"
  }
];

const evaluationLayers = [
  ["单算子正确性", "分别验证 ReshapeAndCache、Paged Attention 与 Sparse Attention 的数值语义"],
  ["组合正确性", "按环境变量切换官方、Triton 稠密与 Sparse 实现，逐段定位集成问题"],
  ["Offline Throughput", "在固定 prompt、输入输出长度和运行参数下比较受控吞吐"],
  ["Serving Benchmark", "测量并发服务中的请求吞吐、TTFT、TPOT、ITL 与失败请求"]
];

const highlights = [
  ["100", "LongBench prompts"],
  ["~8K", "平均输入 tokens"],
  ["~2×", "Sparse / Triton Dense"],
  ["40960", "验证上下文 tokens"]
];
---

<SiteLayout
  title="Ascend Kernel-to-Serving Evaluation Pipeline"
  description="在首届算子开发创新大赛中，从 Triton-Ascend baseline、Paged Attention 和 Quest Sparse Attention，一直做到 vLLM-Ascend 集成与端到端评测。"
  image="/images/ustc-kernel-competition-poster.png"
>
  <header class="project-detail-hero shell reveal">
    <a class="project-back-link" href="/projects/">
      <ArrowLeft size={15} aria-hidden="true" /> 返回项目
    </a>
    <p class="eyebrow">Competition TA / Kernel engineering</p>
    <h1>Ascend Kernel-to-Serving<br />Evaluation Pipeline</h1>

    <div class="project-detail-summary">
      <p class="project-detail-lead">
        在首届算子开发创新大赛中，为一条从基础算子、注意力优化到大模型在线服务的完整链路建立 baseline、集成框架和分层评测体系。
      </p>
      <dl class="project-facts">
        <div><dt>Period</dt><dd>2026.01–04</dd></div>
        <div><dt>Role</dt><dd>Teaching Assistant</dd></div>
        <div><dt>Hardware</dt><dd>Ascend 910B3</dd></div>
        <div><dt>Stack</dt><dd>Triton / vLLM</dd></div>
      </dl>
    </div>
  </header>

  <article class="prose project-article">
    <figure class="article-figure poster-figure">
      <img src="/images/ustc-kernel-competition-poster.png" alt="中国科学技术大学首届算子开发创新大赛项目海报" width="3024" height="4536" loading="lazy" />
      <figcaption>首届算子开发创新大赛项目海报</figcaption>
    </figure>

    <p class="project-article-deck">
      从 1 月到 4 月，我作为助教参与技术支持、赛题设计和评测体系建设。项目以 Ascend 910B3 为硬件平台、Triton-Ascend 为主要开发工具，目标不是完成几个孤立的 kernel，而是让参赛实现真正进入 vLLM-Ascend，并在统一条件下被正确调用、稳定测量和公平比较。
    </p>

    <blockquote>MatMul / Online Softmax → Paged Attention / Quest Sparse Attention → vLLM-Ascend → Correctness / Throughput / Serving</blockquote>

    <h2>项目目标：从功能正确走到真实推理</h2>
    <p>大模型最终需要落到硬件上执行，算子是模型与芯片之间的执行单元。大赛采用逐级递进的设计：先完成环境和基础知识培训，再实现 Softmax 与 MatMul，随后进入 Paged Attention 和 Quest Sparse Attention，最后把算子接入 vLLM-Ascend 做端到端评测。</p>
    <p>到了集成阶段，问题发生了变化。算子在独立测试中输出正确，并不意味着它能够直接进入真实推理系统。变长序列、分页映射、动态 batch、GQA、KV Cache 生命周期、图模式、服务并发和显存预分配，都会改变接口和指标的含义。</p>

    <div class="stack-flow kernel-flow" aria-label="从基础算子到推理系统的工程链路">
      {pipeline.map((item) => (
        <section class="stack-stage">
          <div class="stack-stage-meta"><span>{item.label}</span></div>
          <h3>{item.title}</h3>
          <p>{item.copy}</p>
        </section>
      ))}
    </div>

    <h2>我的工作范围</h2>
    <h3>全流程 baseline 实现</h3>
    <p>我完整实现了 MatMul、Online Softmax、Paged Attention、Quest Sparse Attention 以及 vLLM-Ascend 集成 baseline。它们用于验证题目接口、提供正确性参照、区分 kernel 与框架问题，并为性能评测建立可复现起点。</p>

    <h3>各阶段测评与题面复查</h3>
    <p>基础阶段负责 Softmax 测评并参与部分 MatMul 测评；综合阶段复查并试做 Paged Attention 与 Quest Sparse Attention。亲自走过数据布局、边界条件与运行脚本，才能发现题面歧义并判断性能目标是否合理。</p>

    <h3>独立评测环境</h3>
    <p>我协助准备独立机房的测评服务器与运行环境，固定软件版本、文件布局和运行入口。高性能结果会受到后台负载、首次编译、缓存、模型初始化和测试顺序影响，评测环境本身就是公平比较的一部分。</p>

    <h3>集成阶段框架与脚本</h3>
    <p>这是投入最多的部分，包括集成题面、三个算子接入框架、vLLM-Ascend 评测框架、正确性脚本、离线吞吐脚本、Serving Benchmark、数据集构造、CLI 与 server 两种入口、NPU Graph 兼容检查，以及答辩重新提交阶段的长序列和极限长度复测。</p>

    <h2>三个算子，一条推理链路</h2>
    <h3>ReshapeAndCache：正确写入分页 KV Cache</h3>
    <p>新 token 的 Key 和 Value 最初连续排列，而 Paged Attention 使用按物理块组织的 KV Cache。ReshapeAndCache 根据 <code>slot_mapping</code> 计算物理块和块内偏移，并跳过负值映射。这个算子计算不复杂，但地址一旦写错，后续 Attention 读取到的就是错误上下文。</p>
    <pre><code>block_idx    = slot_idx // block_size
block_offset = slot_idx % block_size</code></pre>

    <h3>GQA Paged Attention：在非连续缓存上稳定计算</h3>
    <p>集成版需要支持 GQA、逻辑块到物理块的映射、batch 内不同的实际上下文长度，以及最后一个 block 的无效 token mask。Online Softmax 避免显式保存完整 score，但必须在分块最大值变化时正确重标定旧的归一化系数与累积输出。</p>

    <h3>Quest Sparse Attention：先选块，再精确计算</h3>
    <p>Quest 先为逻辑块打分并选择 Top-K，再只对候选块执行精确注意力。集成实现同时处理 GQA、变长上下文、分页 KV Cache 和无效 token。评分、Top-K、物理块读取和精确注意力之间任何语义偏差，都会在最终模型输出与吞吐中被放大。</p>

    <h2>分层评测体系</h2>
    <div class="article-table-wrap">
      <table>
        <thead><tr><th>层级</th><th>验证目标</th></tr></thead>
        <tbody>{evaluationLayers.map((item) => <tr><td>{item[0]}</td><td>{item[1]}</td></tr>)}</tbody>
      </table>
    </div>
    <p>框架使用 <code>VLLM_KVCACHE_MODE</code> 与 <code>PAGED_ATTN_MODE</code> 在官方 NPU、Triton 稠密和 Sparse 实现之间切换。这样可以只替换一个环节，也可以进行组合测试，比一次性替换整条推理路径更容易定位错误。</p>

    <h3>为什么 Offline 与 Serving 必须分开</h3>
    <p>Offline Throughput 更接近受控条件下的计算吞吐；Serving Benchmark 还会受到调度、并发和请求到达方式影响，需要分别观察 request throughput、token throughput、TTFT、TPOT、ITL、成功请求和峰值并发。两种口径回答的是不同问题，不能混成一个排名指标。</p>

    <h2>长序列 Sparse 测试</h2>
    <p>为了让 Sparse 与 Dense 在更有代表性的长上下文上比较，我从 LongBench 的 Qasper、NarrativeQA 和 GovReport 构造了 100 条 workload 对齐的 prompt，平均输入长度约 8K tokens，并固定请求集合与生成参数。</p>

    <div class="benchmark-highlights">
      {highlights.map((item) => (
        <div><strong>{item[0]}</strong><span>{item[1]}</span></div>
      ))}
    </div>

    <p>在这组数据上，Quest Sparse Attention 的端到端性能约为 Triton Paged Attention 的 2 倍；它大致达到官方 Ascend C Paged Attention 的 70%，而当前 Triton 稠密实现约为官方实现的 30%。这些是端到端近似值，会随批次、调度和统计口径波动，不能把四舍五入后的比例机械相乘。</p>
    <p>更重要的结论是：长序列上动态稀疏开始体现减少 KV 读取和注意力计算的收益，但评分、Top-K、索引和多 kernel 调度仍有额外成本；后续优化重点应放在融合、数据局部性和减少中间结果。</p>

    <h2>三个容易被误判的问题</h2>
    <h3>算子长度不等于模型长度</h3>
    <p>当前 Qwen3-0.6B 配置下，整条 Sparse Attention 推理链路验证到 40960 tokens，也就是该模型配置支持的最长上下文。这个结果证明链路到达模型上限，但不等价于 kernel 本身的理论上限。</p>

    <h3>算法近似误差不等于实现误差</h3>
    <p>Sparse Attention 不应直接与完整稠密注意力逐元素相等。评测需要固定 Quest 评分、Top-K 选择和精确注意力参考流程，再比较 Triton 输出；否则无法判断差异来自稀疏策略还是 kernel 实现。</p>

    <h3>vLLM 预分配会改变显存指标</h3>
    <p>vLLM 会根据 <code>--gpu-memory-utilization</code> 尽可能预分配剩余显存给 KV Cache，所以不同长度数据集可能显示相近的总显存占用。Quest 优化的是注意力访问和计算路径，并不会天然压缩完整 KV Cache。更可信的评测应分别观察常驻显存、KV Cache 容量、kernel 临时峰值、固定预算下的最大并发与稀疏工作区。</p>

    <h2>NPU Graph 与两种运行入口</h2>
    <p>eager 模式通过不代表算子可以进入图模式。动态图形状、Python 控制流、张量生命周期和编译行为都会影响图捕获，因此我补充了入图兼容脚本。同时保留 CLI 离线吞吐与 server + client 服务并发两种入口，用于区分 kernel、scheduler 和服务配置带来的性能变化。</p>

    <h2>项目结果与边界</h2>
    <p>项目海报统计显示：环境配置阶段有 46 人参与，综合算子阶段累计收到 65 人次提交，最终 15 人完成集成测试。团队汇总结果中，Paged Attention 阶段最高达到初始 baseline 的 850.2 倍，Sparse Attention 阶段最高达到 66 ms baseline 的 3.46 倍。</p>
    <blockquote>这些数字是参赛选手与整个项目团队的共同成果，不是我的个人性能成绩。我的工作重点是让不同实现能够在统一环境、统一数据和统一口径下被正确比较。</blockquote>

    <h2>这段经历带给我的认识</h2>
    <ul>
      <li><strong>出题本身也是系统设计。</strong>接口、布局、参考语义、容差、边界条件和性能口径必须同时明确。</li>
      <li><strong>正确性测试要能定位。</strong>分层切换官方与 Triton 实现，才能判断问题位于缓存写入、分页读取、稀疏选择还是框架调用。</li>
      <li><strong>性能测试先统一语义。</strong>输入、长度、并发、预热、缓存和统计口径不一致时，结果没有解释力。</li>
      <li><strong>框架行为会改变指标含义。</strong>理解内存预分配、调度和图捕获，与理解 kernel 本身同样重要。</li>
      <li><strong>从 kernel 到 serving 不会自动成立。</strong>数据准备、Top-K、kernel launch、图模式和 scheduler 都可能抵消 microbenchmark 的局部收益。</li>
    </ul>

    <h2>结语</h2>
    <p>这四个月的工作横跨算子实现、题目设计、测试框架、服务器环境、数据集与端到端推理。我做的不只是几道算子题，而是为一条从算子到大模型服务的完整链路建立了可实现、可测试、可比较的工程基线。</p>
    <p>下一步最值得继续研究的方向，是减少评分与 Top-K 的额外开销、推进 kernel 融合、提高 KV 数据局部性，并在更长上下文和更高并发下重新验证端到端收益。</p>

    <h2>参考资料</h2>
    <ol>
      <li><a href="https://ustc-kernel-innovation.github.io/homepage/" target="_blank" rel="noreferrer">中国科学技术大学首届算子开发创新大赛主页</a></li>
      <li><a href="https://ustc-kernel-innovation.github.io/homepage/junior/" target="_blank" rel="noreferrer">基础算子开发页面</a></li>
      <li><a href="https://ustc-kernel-innovation.github.io/homepage/senior/" target="_blank" rel="noreferrer">综合算子开发页面</a></li>
      <li><a href="https://github.com/Ascend/triton-ascend" target="_blank" rel="noreferrer">Triton-Ascend 官方项目</a></li>
    </ol>
  </article>
</SiteLayout>
